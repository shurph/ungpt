status: backlog

---

Нужно изменить логику `Service Worker` в PWA, чтобы после деплоя приложение быстрее получало свежую версию, но при этом сохраняло офлайн-работу.

Что нужно сделать:

1. Проанализировать текущий `sw.js` и найти место, где для всех `GET` запросов используется стратегия `cache-first` или эквивалентная ей логика.

2. Изменить стратегию кэширования:

   * для `index.html`, основного `app.js` и при необходимости других критичных файлов входа приложения использовать:

     * `network-first` с fallback на кэш,
       либо
     * `stale-while-revalidate`, если это лучше ложится в текущую архитектуру;
   * для остальной статической статики оставить текущую стратегию `cache-first`:

     * изображения,
     * шрифты,
     * CSS,
     * иконки,
     * прочие неизменяемые ассеты.

3. Обеспечить корректное офлайн-поведение:

   * если приложение уже было загружено ранее и файлы есть в кэше, то при отсутствии интернета PWA должно открываться из кэша;
   * если сеть недоступна, `index.html` и `app.js` должны браться из кэша как fallback;
   * не ломать существующую офлайн-поддержку.

4. Сохранить и при необходимости доработать очистку старых кэшей:

   * использовать версионирование через `CACHE_NAME`;
   * при изменении логики обновить имя кэша;
   * в `activate` удалять старые кэши, не совпадающие с текущим `CACHE_NAME`.

5. Проверить сценарий после деплоя:

   * при наличии интернета пользователь должен получать новую версию быстрее, чем при старой схеме `cache-first` для всех `GET`;
   * при отсутствии интернета должна открываться последняя успешно закэшированная версия приложения;
   * новая версия не обязана становиться доступной офлайн до тех пор, пока хотя бы один раз не была успешно загружена из сети.

Что ожидаю в ответе:

* показать итоговый изменённый код `sw.js`;
* кратко пояснить, какие запросы теперь идут по `network-first` или `stale-while-revalidate`, а какие остаются на `cache-first`;
* отдельно описать, как будет вести себя приложение:

  * при обычном онлайне,
  * сразу после деплоя,
  * при открытии без интернета.
